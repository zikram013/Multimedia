
 $(document).ready(function() {
       //Call a variable to know the width of the window
       var screenWidth = $(window).width();
       $('barraDeOpciones').css('width', screenWidth + 'px');
    });
